{
    "name": "naratour",
    "version": "1.0.0",
    "description": "나라투어 웹사이트",
    "main": "index.js",
    "scripts": {
        "dev": "netlify dev",
        "build": "mkdir -p dist && cp *.html dist/ && cp -r assets dist/ && cp -r pages dist/ && cp aws-exports.js dist/ && cp amplifyconfiguration.json dist/ && echo 'Build completed to dist folder!'",
        "start": "echo 'Static website ready to serve from dist'"
    },
    "dependencies": {
        "axios": "^1.10.0",
        "dotenv": "^16.5.0",
        "openai": "^4.96.0"
    },
    "devDependencies": {
        "netlify-cli": "^15.0.0"
    }
}
