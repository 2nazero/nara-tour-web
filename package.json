{
    "name": "naratour",
    "version": "1.0.0",
    "description": "나라투어 웹사이트",
    "main": "index.js",
    "scripts": {
        "dev": "netlify dev",
        "build": "mkdir -p dist && cp *.html dist/ && cp -r assets dist/ && cp -r pages dist/ && cp -r data dist/ && cp -r api dist/ && echo 'Build completed to dist folder!'",
        "start": "echo 'Static website ready to serve from dist'"
    }
}
